{% extends 'base.html.twig' %}

{% block body %}
  <h1 class="text-4xl font-bold text-center mb-4">Nouveau Cours</h1>
  
  <div class="mx-auto max-w-lg">
    {{ form(form) }}
  </div>

{% endblock %}
