{% extends 'base.html.twig' %}

{% block title %}Nouvelle Leçon{% endblock %}

{% block body %}
  <h1 class="text-4xl font-bold text-center mb-4">Création d'une nouvelle Leçon</h1>
  
  <div class="mx-auto max-w-lg">
    {{ form(form) }}
  </div>

  <a href="{{ path('home_page') }}">Revenir à l'accueil</a>
{% endblock %}