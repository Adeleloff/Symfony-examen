{% extends 'base.html.twig' %}

{% block body %}
  <h1 class="text-4xl font-bold text-center mb-4">Inscription d'un Enseignant</h1>
  
  <div class="mx-auto max-w-lg">
    {{ form(registrationForm) }}
  </div>

  <div class="text-center mt-8">
    <a href="{{ path('teacher_index') }}" class="px-4 py-2 bg-blue-500 text-white font-semibold rounded-lg shadow-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:ring-opacity-75">
      Retour à la liste de nos Enseignants
    </a>
  </div>

{% endblock %}
