<form method="post" action="{{ path('subcategory_delete', {'id': sub_category.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette Sous-catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_category.id) }}">
    <button class="btn">Supprimer</button>
</form>
